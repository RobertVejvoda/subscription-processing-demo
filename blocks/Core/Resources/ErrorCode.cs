namespace Core.Resources
{
    public static class ErrorCode
    {
        public const string INVALID_FILE = nameof(INVALID_FILE);
        public const string PARSING_ERROR = nameof(PARSING_ERROR);
        public const string DUPLICATE_VALUE = nameof(DUPLICATE_VALUE);
        public const string INVALID_VALUE = nameof(INVALID_VALUE);
        public const string INVALID_VALUE_FOR_SUBSCRIPTION = nameof(INVALID_VALUE_FOR_SUBSCRIPTION);
    }
}